%section.section
  .container
    - title "Edit Application"
    .form-container
      %h1.section-title
        Edit
        %span.emphasized Application

    = render 'form'

    - if @questionnaire.valid?
      %p.cancel_btn_wrap= link_to 'Cancel', questionnaires_path
